Applica le seguenti modifiche al sito esistente, senza toccare struttura, styling o altre sezioni:

1. Contatti

Rimuovi completamente il numero di telefono dalla sezione contatti (sia dal blocco phone/phone_label che da qualsiasi altro punto del sito in cui compaia, inclusi eventuali link tel:)
Offusca l'indirizzo email per proteggerlo dallo scraping automatico: invece di inserirlo come testo puro in HTML, ricomponilo via JavaScript al caricamento della pagina (es. concatenando user e domain separatamente in JS e inserendoli nel DOM solo a runtime), oppure usa la codifica HTML entity dei caratteri, in modo che non sia leggibile come stringa diretta nel sorgente HTML statico
L'email deve restare cliccabile/funzionante per l'utente reale (link mailto: generato dinamicamente), ma non deve comparire come testo semplice nel codice sorgente

2. Posizione/Mappa

Modifica le coordinate usate nell'iframe/embed di Google Maps: invece del punto esatto della struttura (via Don Sapino 7), centra la mappa su un punto approssimativo a 200-300 metri di distanza (es. verso la Reggia di Venaria), mantenendo comunque la zona chiaramente riconoscibile
Nel testo della sezione "location", mantieni il riferimento alla zona (Venaria Reale, vicinanza alla Reggia) ma non specificare il numero civico esatto

Inoltre, sistema le icone dei servizi offerti. L'icona della telecamera è un quadrato pieno, troppo simile all'icona della tv oltretutto. Da sistemare inoltre quella della lavatrice e del parcheggio.

Non modificare nessun altro contenuto, sezione, blocco di testo o elemento di stile già presente. Alla fine conferma quali file hai modificato.
